{
  "name": "resume-generator-studio",
  "version": "1.0.0",
  "description": "A Render-ready resume generator and editor with AI-ready prompt building and PDF export.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.3"
  }
}
