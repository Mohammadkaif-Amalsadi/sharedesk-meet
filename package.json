{
  "name": "forex-movers-dashboard",
  "version": "1.0.0",
  "description": "A Render-ready forex movers dashboard for top gaining and losing currency pairs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.3"
  }
}
