{
  "name": "sharedesk-meet",
  "version": "1.0.0",
  "description": "A Render-ready WebRTC meeting app with host-approved shared-screen interaction events.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.3",
    "socket.io": "^4.7.5"
  }
}
